# Nornir logs
nornir.log

# Log directories — too noisy, not useful in git
logs/

# Baseline — environment specific, regenerate per lab
baseline.json

# Python cache
__pycache__/
*.pyc
*.pyo

# Local secrets — never commit
.env

# Rendered configs contain real secrets post-render — regenerate via render.py
rendered/
